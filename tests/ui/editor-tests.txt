(async()=>{
 const results=[];const check=(name,ok)=>{if(!ok)throw Error(name);results.push(name)};
 const q=s=>ui.shadow.querySelector(s);const click=s=>{const e=q(s);if(!e)throw Error('Missing '+s);e.click()};
 const field=(key,value)=>{const el=q('[data-action="prompt-field"][data-field="'+key+'"]');if(!el)throw Error('Missing field '+key);if(el.type==='checkbox')el.checked=value;else el.value=value;el.dispatchEvent(new Event(el.tagName==='SELECT'||el.type==='checkbox'?'change':'input',{bubbles:true}));};
 const unlock=()=>{const el=q('[data-action="edit-mode"]');el.checked=true;el.dispatchEvent(new Event('change',{bubbles:true}));};
 const original=structuredClone(data);const savedOriginal=structuredClone(stored);
 await ui.settle();ui.closePanel();ui.openPanel();await ui.settle();ui.state.activeTab='advanced';ui.render();
 check('编辑总开关默认关闭且没有局部解锁',!ui.state.editorUnlocked&&!q('[data-action="editor-unlock"]')&&!q('[data-action="prompt-new"]'));
 const id=ui.IDS.resetCache;ui.openPromptEditor(id);
 check('完整正文只读可复制',q('[data-field="content"]').readOnly&&!q('[data-field="content"]').disabled&&q('[data-field="content"]').value===data.prompts.find(p=>p.id===id).content);
 check('只读不允许保存或修改角色',q('[data-action="prompt-save"]').disabled&&q('[data-field="role"]').disabled);
 const lockedBefore=JSON.stringify(data);ui.setEditorField('name','绕过锁定');await ui.savePromptEditor();check('逻辑层同样执行编辑锁',JSON.stringify(data)===lockedBefore);
 unlock();check('解锁后名称正文角色可编辑',!q('[data-field="content"]').readOnly&&!q('[data-field="role"]').disabled);
 field('name','缓存排障');field('content','测试正文\n{{原样宏}}\n<xml>原样内容</xml>');field('role','assistant');field('enabled',true);field('ordinal','4');
 await ui.savePromptEditor();await ui.settle();
 check('保存成功后自动关闭条目编辑器',!ui.state.promptEditor&&!q('.prompt-editor'));
 let p=data.prompts.find(p=>p.id===id);
 check('名称正文角色启用状态同步当前与保存预设',p.name==='缓存排障'&&p.role==='assistant'&&p.enabled&&p.content.includes('{{原样宏}}')&&JSON.stringify(p)===JSON.stringify(stored.prompts.find(p=>p.id===id)));
 check('列表顺序调整与原生一致',data.prompts[3].id===id&&stored.prompts[3].id===id);
 ui.openPromptEditor(id);field('positionType','in_chat');field('depth','7');field('order','22');await ui.savePromptEditor();
 check('聊天深度和同层顺序可编辑',JSON.stringify(data.prompts.find(p=>p.id===id).position)===JSON.stringify({type:'in_chat',depth:7,order:22}));
 ui.openPromptEditor(id);data.prompts.find(p=>p.id===id).name='原生改名';ui.reconcilePreset('测试原生修改');
 check('无草稿时原生改名立即同步弹层',q('[data-field="name"]').value==='原生改名');
 field('content','草稿正文');data.prompts.find(p=>p.id===id).role='user';ui.reconcilePreset('外部改角色');
 check('外部刷新不覆盖未保存正文',q('[data-field="content"]').value==='草稿正文');
 await ui.savePromptEditor();check('合并未编辑字段并保存原生未保存名称',data.prompts.find(p=>p.id===id).role==='user'&&stored.prompts.find(p=>p.id===id).name==='原生改名'&&stored.prompts.find(p=>p.id===id).content==='草稿正文');
 ui.openPromptEditor(id);field('content','冲突草稿');data.prompts.find(p=>p.id===id).content='外部正文';ui.reconcilePreset('正文冲突');await ui.savePromptEditor();
 check('正文并发冲突阻止覆盖且保留草稿',data.prompts.find(p=>p.id===id).content==='外部正文'&&q('[data-field="content"]').value==='冲突草稿'&&ui.state.promptEditor.message.includes('已被'));
 ui.closePromptEditor();check('有草稿关闭先内联确认',!!q('[data-action="prompt-discard"]'));click('[data-action="prompt-continue"]');
 click('[data-action="prompt-reload"]');check('重新载入先保留草稿并提示',ui.state.promptEditor.dirty&&ui.state.promptEditor.message.includes('再次点击'));click('[data-action="prompt-reload"]');
 check('明确重新载入后读取最新内容',!ui.state.promptEditor.dirty&&q('[data-field="content"]').value==='外部正文');
 field('ordinal','5');const first=data.prompts.shift();data.prompts.push(first);ui.reconcilePreset('外部调整顺序');await ui.savePromptEditor();
 check('并发顺序变化不会被草稿覆盖',data.prompts.at(-1).id===first.id&&ui.state.promptEditor.dirty&&ui.state.promptEditor.message.includes('顺序'));
 ui.closePromptEditor(true);ui.openPromptEditor('worldInfoBefore');
 check('必需条目不能禁用或移出列表',q('[data-field="enabled"]').disabled&&q('[data-field="included"]').disabled);
 check('占位符正文不会伪装成可编辑静态文本',q('[data-field="content"]').readOnly);
 ui.setEditorField('enabled',false);await ui.savePromptEditor();check('必需条目保存层同样保护',data.prompts.find(p=>p.id==='worldInfoBefore').enabled&&ui.state.promptEditor.message.includes('必需'));
 ui.closePromptEditor(true);ui.openPromptEditor(ui.GROUPS['variable-mode'].options[0][0]);
 check('变量模式编辑保留世界书联动入口',q('[data-field="enabled"]').disabled&&q('.editor-note').textContent.includes('联动'));
 ui.closePromptEditor();ui.openPromptEditor();ui.closePromptEditor();check('未修改的新建条目可直接关闭',!ui.state.promptEditor);ui.openPromptEditor();field('name','测试新条目');field('content','新内容');const created=await ui.savePromptEditor();
 check('新增条目默认关闭且同步到酒馆',!!created&&data.prompts.at(-1).id===created&&!data.prompts.at(-1).enabled&&stored.prompts.at(-1).id===created);
 ui.openPromptEditor(created);field('included',false);await ui.savePromptEditor();check('移出列表保留在未加入区',!data.prompts.some(p=>p.id===created)&&data.prompts_unused.some(p=>p.id===created));
 ui.closePromptEditor();click('[data-action="entry-filter"][data-value="unused"]');check('未加入条目可检索编辑',!!q('[data-action="prompt-open"][data-id="'+created+'"]'));
 ui.openPromptEditor(created);field('included',true);field('ordinal','2');await ui.savePromptEditor();check('未加入条目可重新加入指定位置',data.prompts[1].id===created&&!data.prompts_unused.some(p=>p.id===created));
 ui.openPromptEditor(created);field('name','失败测试');const before=structuredClone(stored);window.failWrite='in_use';await ui.savePromptEditor();
 check('保存失败回滚持久预设并保留草稿',JSON.stringify(stored)===JSON.stringify(before)&&ui.state.promptEditor.dirty&&q('[data-field="name"]').value==='失败测试');
 ui.closePromptEditor(true);ui.openPromptEditor(created);field('content','跨预设草稿');const loaded=getLoadedPresetName;window.getLoadedPresetName=()=> 'another';ui.reconcilePreset('切换预设');await ui.savePromptEditor();
 check('切换预设自动锁定并阻止旧草稿保存',!ui.state.editorUnlocked&&ui.state.promptEditor.contextChanged&&data.prompts.find(p=>p.id===created).content==='新内容');window.getLoadedPresetName=loaded;
 ui.closePromptEditor(true);ui.state.activeTab='tools';ui.render();check('重置命中缓存独立功能且无其他可用板块',!!q('[data-key="prompt:'+id+'"]')&&!q('.content').textContent.includes('其他可用设置'));
 for(const tab of ['daily','style','tools']){ui.state.activeTab=tab;ui.render();check(tab+' 不展示必需条目开关',[...ui.PROTECTED_IDS].every(id=>!q('[data-key="prompt:'+id+'"]')));}
 ui.state.editorUnlocked=true;ui.closePanel();ui.openPanel();await ui.settle();check('关闭再打开自动恢复锁定',!ui.state.editorUnlocked);
 data=original;stored=savedOriginal;ui.reconcilePreset('还原测试');ui.state.activeTab='advanced';ui.state.entryFilter='all';ui.state.search='';ui.render();
 return results;
})()
